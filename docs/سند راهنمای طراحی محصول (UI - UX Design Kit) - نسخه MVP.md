## ۱. اصول بنیادی طراحی (Design Principles)

با توجه به ماهیت محصول (LegalTech) و مخاطب عام، اصول زیر باید در تمام صفحات رعایت شوند:

1. **وضوح بر زیبایی (Clarity over Aesthetics):** کاربر برای حل یک مشکل پیچیده آمده است. هیچ المان گرافیکی نباید مانع خوانایی متن شود.


2. **آرامش‌بخشی (Calming):** استفاده از فضای سفید زیاد و رنگ‌های سرد برای کاهش استرس کاربرانی که درگیر توهین یا خیانت در امانت شده‌اند.
3. **هدایت‌گری (Guidance):** کاربر حقوق‌دان نیست. سیستم باید در هر مرحله توضیح دهد چرا این اطلاعات (مثلاً کد ملی طرف مقابل) را می‌خواهد.


4. 
**شفافیت حقوقی (Legal Transparency):** سلب مسئولیت‌ها (Disclaimers) باید بخشی از طراحی باشند، نه متن‌های ریز و پنهان.



---

## ۲. پالت رنگی (Color Palette)

رنگ‌ها باید حس «قانون»، «تکنولوژی» و «اعتماد» را منتقل کنند.

| نام رنگ | کد رنگ (HEX) | کاربرد | روانشناسی |
| --- | --- | --- | --- |
| **آبی لاجوردی (Primary)** | `#1A3A5F` | دکمه‌های اصلی، هدر، برندینگ | نماد اقتدار، قانون و اعتماد (جایگزین رنگ مشکی مطلق). |
| **آبی آسمانی (Secondary)** | `#4A90E2` | دکمه‌های ثانویه، لینک‌ها، آیکون‌ها | حس تکنولوژی و هوش مصنوعی. |
| **سفید کاغذی (Background)** | `#F9FAFB` | پس‌زمینه صفحات | شبیه‌سازی حس کاغذ و اسناد رسمی، کاهش خستگی چشم. |
| **سبز کله‌غازی (Success)** | `#27AE60` | پیام‌های موفقیت، تایید مدارک | القای حس حل شدن مشکل و آرامش. |
| **نارنجی هشدار (Warning)** | `#F39C12` | سلب مسئولیت‌ها، نکات مهم حقوقی | توجه را جلب می‌کند اما مثل قرمز (خطر) استرس‌زا نیست.

 |
| **خاکستری تیره (Text)** | `#333333` | متون اصلی | خوانایی بالا (کنتراست استاندارد WCAG). |

---

## ۳. تایپوگرافی (Typography)

از آنجا که محصول بر پایه «متن و سند» است، انتخاب فونت حیاتی است.

* **فونت اصلی (UI):** `Vazirmatn` یا `Yekan Bakh` (وزن‌های Regular و Bold).
* دلیل: خوانایی بالا در موبایل، اعداد فارسی استاندارد و ظاهر مدرن.


* **فونت اسناد (Document Preview):** `B Nazanin` یا `B Zar`.
* دلیل: شبیه‌سازی ظاهر نامه‌های اداری و قضایی ایران تا کاربر حس کند یک سند رسمی دریافت کرده است.




* **سایزها:**
* عنوان‌ها: 18px - 22px (Bold)
* متن بدنه و چت: 14px - 16px (Regular)
* توضیحات ریز (Disclaimers): 12px (Light)



---

## ۴. تجربه کاربری (UX) و جریان‌های کلیدی

بر اساس استراتژی MVP و سند تحقیقاتی، ۳ جریان اصلی باید طراحی شود:

### ۴.۱. جریان ورود و احراز هویت (Onboarding)

* **قانون:** حذف فرم‌های طولانی ثبت‌نام.
* **طراحی:**
1. صفحه خوش‌آمدگویی با شعار ساده (مثلاً: تنظیم شکوائیه در ۵ دقیقه).
2. فیلد شماره موبایل (فقط عدد).
3. دریافت کد OTP.
4. 
**نکته مهم:** دریافت نام، نام پدر و کد ملی در این مرحله **ممنوع** است و باید به مرحله نهایی (تولید سند) موکول شود تا نرخ ریزش (Churn) کم شود.





### ۴.۲. جریان چت‌بات ترایاژ (Triage Chatbot)

این قلب محصول شماست. کاربر نباید حس کند فرم پر می‌کند، بلکه باید حس کند با یک مشاور صحبت می‌کند.

* **رابط کاربری (UI):**
* المان‌های چت (Bubble) در سمت راست (کاربر) و چپ (هوش مصنوعی).
* آواتار هوش مصنوعی: یک نماد انتزاعی (نه عکس انسان) تا تظاهر به وکالت تلقی نشود.




* **منطق تعامل:**
* 
**انتخاب سناریو:** دکمه‌های گزینه‌ای (Chips) برای انتخاب نوع جرم: «خیانت در امانت»، «توهین»، «مزاحمت».


* **درخت تصمیم:** اگر کاربر «خیانت در امانت» را انتخاب کرد، بات باید بپرسد: «آیا قراردادی دارید که نشان دهد مال را امانت داده‌اید؟» (بله/خیر) .


* 
**تشخیص توهین:** اگر کاربر الفاظ رکیک وارد کرد، سیستم باید آن‌ها را دریافت کند اما در نمایشگر با کاراکترهای `***` نمایش دهد (فیلترینگ محتوا).





### ۴.۳. جریان تولید و تحویل سند (Document Engine)

* 
**فرم‌ساز هوشمند:** پس از پایان چت، یک فرم نهایی برای اطلاعات دقیق (تاریخ دقیق، آدرس پستی، کد پستی) نمایش داده شود.


* **پیش‌نمایش:** نمایش تار (Blurred) یا واترمارک‌دار فایل PDF شکوائیه قبل از پرداخت.
* **صفحه موفقیت (Success Page):**
* دکمه دانلود فایل Word و PDF.


* نقشه گوگل (Map) که نزدیک‌ترین «دفتر خدمات الکترونیک قضایی» را نشان می‌دهد.


* یک چک‌لیست (Checklist) گرافیکی از مدارکی که کاربر باید همراه خود ببرد (کارت ملی، پرینت پیامک و...).





---

## ۵. کامپوننت‌های اختصاصی (UI Kit Components)

### الف) کارت سلب مسئولیت (The Disclaimer Component)

این کامپوننت باید در فوتر تمام صفحات و ابتدای چت به صورت `Sticky` یا `Modal` وجود داشته باشد.

* **رنگ:** پس‌زمینه زرد کم‌رنگ (`#FFF3CD`) با متن تیره.
* **متن:** «این سامانه وکیل دادگستری نیست و صرفاً پیش‌نویس اوراق قضایی را تنظیم می‌کند.»
* **آیکون:** ⚠️ (Warning Icon).

### ب) سلکتور نوع جرم (Crime Selector)

برای صفحه اول چت‌بات:

* کارت‌هایی با آیکون‌های خطی (Outline Icons):
* 🗣️ **توهین و فحاشی** (زیرنویس: فضای مجازی یا حقیقی)
* 🤝 **خیانت در امانت** (زیرنویس: خودرو، چک، سفته)
* 📞 **مزاحمت** (زیرنویس: تلفنی، بانوان، اطفال).





### ج) دکمه‌های اقدام (CTA)

* متن دکمه‌ها نباید «مشاوره با وکیل» باشد (ریسک حقوقی).
* متن مجاز: «تنظیم هوشمند شکوائیه»، «دریافت فایل نهایی»، «بررسی مدارک».



---

## ۶. ملاحظات موبایل و ریسپانسیو

بیشترین کاربران این سرویس از موبایل استفاده می‌کنند.

* **Input Fields:** کیبورد مناسب برای هر فیلد (مثلاً کیبورد عددی برای کد ملی و تاریخ).
* **Touch Targets:** ارتفاع دکمه‌ها حداقل 48px باشد.
* 
**Bottom Sheets:** برای نمایش توضیحات حقوقی (مثلاً توضیح اینکه "توهین مشدد چیست") از منوهای کشویی پایین صفحه استفاده کنید تا کاربر از چت خارج نشود.



---

## ۷. خروجی مورد انتظار برای توسعه‌دهنده

در طراحی نهایی (فیگما)، صفحات زیر باید تحویل داده شوند:

1. **Splash Screen:** لوگو + شعار.
2. **Login:** ورودی شماره موبایل + ورودی کد تایید.
3. **Chat Interface:** شامل حالات مختلف پیام (متن، گزینه‌های چندانتخابی، دریافت فایل).
4. 
**Information Form:** فرم دریافت مشخصات شاکی و متشاکی (با هندل کردن حالت "مجهول‌المکان").


5. **Payment/Preview:** پیش‌نمایش سند + درگاه پرداخت.
6. 
**Result Page:** دانلود فایل‌ها + نقشه دفاتر قضایی + چک‌لیست مدارک.



آیا مایل هستید برای یکی از صفحات (مثلاً صفحه چت‌بات) یک وایرفریم متنی دقیق (Text Wireframe) بنویسم که ترتیب سوالات را مشخص کند؟